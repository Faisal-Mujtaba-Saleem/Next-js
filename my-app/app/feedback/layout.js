export const metadata = {
    title: "Feedback - Feedback page of My first Next.js app",
    description: "This is the feedback page of my first Next.js app",
};

export default function FeedbackLayout({ children }) {
    return (
        <section>
            {children}
        </section>
    )
}